[tool.poetry]
name = "ghostos"
version = "0.0.1"
description = "An agent framework that offers a Python code interface, functioning as an operating system for agents and meta-agents driven by large language models."
authors = ["zhuming <thirdgerb@gmail.com>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

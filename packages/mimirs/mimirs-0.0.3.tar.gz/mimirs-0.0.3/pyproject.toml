[build-system]
requires = [
    "setuptools>=61.0.0",
]
build-backend = "setuptools.build_meta"

[project]
name = "mimirs"
version = "0.0.3"
description = "Mimir is a dict-subclass that calculates missing values through its well attribute."
readme = "README.rst"
classifiers = [
    "Programming Language :: Python",
    "Programming Language :: Python :: 3",
]
keywords = []
dependencies = []
requires-python = ">=3.11"

[project.urls]
Download = "https://pypi.org/project/mimirs/#files"

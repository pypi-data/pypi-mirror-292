[project]
name = "{{ cookiecutter.project_slug }}"
version = "1.0.0"
description = "Convention app for {{ cookiecutter.convention_name }}"
authors = []
dependencies = [
    "nomnom~=1.0.0",
]
requires-python = ">=3.12"
readme = "README.md"
license = {text = "MIT"}

[tool.ruff.lint.isort]
known-first-party = ["{{ cookiecutter.__project_package }}"]

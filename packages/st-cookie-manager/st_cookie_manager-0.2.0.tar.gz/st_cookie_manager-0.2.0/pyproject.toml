[tool.poetry]
name = "st-cookie-manager"
version = "0.2.0"
description = "Access and save cookies from Streamlit"
authors = ["xiaoqinglin <xiaoqinglin2018@gmail.com>"]
license = "Apache-2.0"

readme = "README.md"

include = [
    "st_cookie_manager/build/**/*",
]

[tool.poetry.dependencies]
python = "^3.12"
streamlit = "^1.37.1"
cryptography = "*"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

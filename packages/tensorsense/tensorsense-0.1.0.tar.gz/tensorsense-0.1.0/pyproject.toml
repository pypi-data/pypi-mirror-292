[project]
name = "tensorsense"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "transformers==4.44.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

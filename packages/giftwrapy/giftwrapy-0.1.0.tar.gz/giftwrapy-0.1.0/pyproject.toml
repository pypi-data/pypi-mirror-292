[project]
name = "giftwrapy"
version = "0.1.0"
description = "A simply python package to wrap all directory contents into a single text file to be used with AI assistants."
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

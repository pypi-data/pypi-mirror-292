from dagshub.data_engine.model.datasource import Datasource


def import_yolo(datasource: Datasource, label_folder="labels", class_file="data.yaml"):
    """
    Imports YOLO annotations to

    :param datasource: Datasource with that you want to import the annotations for
    :param label_folder: Name of the folder with annotations.
    :param class_file: Path to the file with classes inside the repo
    """
    pass

[project]
name = "ingot"
version = "0.0.0"
description = "Coming soon..."
requires-python = ">= 3.10"
dependencies = []

[build-system]
requires = [
    "pdm-backend",
]
build-backend = "pdm.backend"

[tool]
